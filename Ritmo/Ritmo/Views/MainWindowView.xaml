<Window x:Class="Ritmo.Views.MainWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Ritmo"
        xmlns:System="clr-namespace:System;assembly=mscorlib"
        xmlns:viewmodels="clr-namespace:Ritmo.ViewModels"
        xmlns:views="clr-namespace:Ritmo.Views"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance viewmodels:MainWindowViewModel, IsDesignTimeCreatable=True}"
        Left="5" Top="5"
        Title="Ritmo" Height="450" Width="800" MinWidth="1000" MinHeight="600">

    <Window.Resources>
        <!--#Region Variables -->
        <!--#endregion-->

        <!--#region ViewModel DataTemplates-->
        <DataTemplate x:Name="homeViewTemplate" DataType="{x:Type viewmodels:HomeViewModel}">
            <views:HomeView DataContext="{Binding}"/>
        </DataTemplate>
        <DataTemplate x:Name="searchViewTemplate" DataType="{x:Type viewmodels:SearchViewModel}">
            <views:SearchView DataContext="{Binding}"/>
        </DataTemplate>
        <DataTemplate x:Name="categoriesViewTemplate" DataType="{x:Type viewmodels:CategoriesViewModel}">
            <views:CategoriesView DataContext="{Binding}"/>
        </DataTemplate>
        <DataTemplate x:Name="followingViewTemplate" DataType="{x:Type viewmodels:FollowingViewModel}">
            <views:FollowingView DataContext="{Binding}"/>
        </DataTemplate>
        <DataTemplate x:Name="AllPlaylistsViewTemplate" DataType="{x:Type viewmodels:AllPlaylistsViewModel}">
            <views:MyPlaylistsView DataContext="{Binding}"/>
        </DataTemplate>
        <DataTemplate x:Name="MyQueueViewTemplate" DataType="{x:Type viewmodels:MyQueueViewModel}">
            <views:MyQueueView DataContext="{Binding}"/>
        </DataTemplate>
        <DataTemplate x:Name="playlistViewTemplate" DataType="{x:Type viewmodels:PlaylistViewModel}">
            <views:PlaylistView DataContext="{Binding}"/>
        </DataTemplate>
        <!--#endregion-->

    </Window.Resources>

    <Grid>
        <!-- Main Layout -->
        <Grid.ColumnDefinitions>
            <ColumnDefinition MaxWidth="170" MinWidth="170" />
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition MinHeight="80" MaxHeight="80"/>
        </Grid.RowDefinitions>

        <!-- Left panel -->
        <Grid Background="Gray" Grid.Row="0" Grid.Column="0">

            <Grid.RowDefinitions>
                <RowDefinition Height="120"/>
                <RowDefinition MinHeight="160" Height="*"/>
                <RowDefinition Height="134"/>
                <RowDefinition Height="134"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="1">
                <Button x:Name="Home_Button" Content="Home" CommandParameter="{Binding HomeViewModel}" Command="{Binding ChangeViewModelCommand}" Style="{StaticResource CustomButtonStyle}" />
                <Button x:Name="Search_Button" Content="Search" CommandParameter="{Binding SearchViewModel}" Command="{Binding ChangeViewModelCommand}" Style="{StaticResource CustomButtonStyle}"/>
                <Button x:Name="Categories_Button" Content="Categories" CommandParameter="{Binding CategoriesViewModel}" Command="{Binding ChangeViewModelCommand}" Style="{StaticResource CustomButtonStyle}"/>
                <Button x:Name="Following_Button"  Content="Following" CommandParameter="{Binding FollowingViewModel}" Command="{Binding ChangeViewModelCommand}" Style="{StaticResource CustomButtonStyle}"/>
            </StackPanel>
            <StackPanel Grid.Row="2">
                <Button x:Name="AllPlaylist_Button" Content="My playlists" CommandParameter="{Binding AllPlaylistsViewModel}" Command="{Binding ChangeViewModelCommand}" Style="{StaticResource CustomButtonStyle}"/>
                <Button x:Name="MyQueue_Button" Content="My Queue" CommandParameter="{Binding MyQueueViewModel}" Command="{Binding ChangeViewModelCommand}" Style="{StaticResource CustomButtonStyle}"/>
            </StackPanel>
        </Grid>

        <!-- Frame view -->
        <Frame Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="1" Grid.RowSpan="1" Content="{Binding CurrentViewModel}"/>

        <!-- Media control -->
        <Button x:Name="PlayButton" Margin="0,0,0,0" Grid.Row="1" Command="{Binding PlayTrackCommand}" Style="{StaticResource MediaButtonStyle}">
            <Image x:Name="PlayButtonIcon" Source="{Binding PlayButtonIcon}"/>
        </Button>
        <Button x:Name="PrevButton" Margin="-120,0,0,0" Command="{Binding PrevTrackCommand}" Style="{StaticResource MediaButtonStyle}">
            <Image Source="/ImageResources/prevtrackbutton.png" Width="30"/>
        </Button>
        <Button x:Name="NextButton" Margin="120,0,0,0" Command="{Binding NextTrackCommand}"  Style="{StaticResource MediaButtonStyle}" Width="30">
            <Image Source="/ImageResources/nexttrackbutton.png"/>
        </Button>
        
        <MediaElement x:Name="WaitingListToQueueTrackElement" HorizontalAlignment="Left" Height="35" Margin="468,11,0,0" VerticalAlignment="Top" Width="42" Grid.Column="1" Grid.Row="1"/>
        <ContentControl Content="{Binding CurrentTrackElement}" Margin="326,11,481,60" Grid.Column="1" Grid.Row="1"/>
        <TextBlock Foreground="Black" VerticalAlignment="Center" Margin="0,11,40,53" Grid.Column="1" Grid.Row="1" HorizontalAlignment="Right" Width="88" Height="16"  >Volume</TextBlock>
        <Slider Name="volumeSlider" VerticalAlignment="Center" Minimum="0" Maximum="1" Value="{Binding CurrentTrackVolume}" Margin="0,32,40,30" Grid.Row="1" Grid.Column="1" Width="100" Height="18" HorizontalAlignment="Right"/>


    </Grid>
</Window>
